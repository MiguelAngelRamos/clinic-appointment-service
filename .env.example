# ============================================================
#  clinic-appointment-service — variables de entorno
# ============================================================

PORT=3005
NODE_ENV=development

# ---- BD propia del appointment-service ---------------------
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=appt_svc_user
DB_PASSWORD=changeme
DB_NAME=appointment_db
DB_SSL=false

# ---- URLs de servicios dependientes (HTTP REST) ------------
# En K8s usa los nombres de los Services internos
PATIENT_SERVICE_URL=http://localhost:3003
DOCTOR_SERVICE_URL=http://localhost:3004

# ---- CORS --------------------------------------------------
ALLOWED_ORIGINS=http://localhost:4200
